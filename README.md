# Li Xin — Work Showcase

A portfolio page: games I built end to end, and the pipelines that let a small
team ship them at volume.

**Live:** https://tlx02.github.io

The page is self-contained — `index.html` plus `assets/`. No build step, no
dependencies. Open `index.html` directly and it works.
